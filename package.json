{
  "name": "winston-batch-transport",
  "version": "1.0.0",
  "description": "A robust Winston transport that batches logs and sends them to a specified API endpoint with support for retries, compression, and concurrent batch processing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "winston",
    "winston-transport",
    "logging",
    "batch",
    "compression",
    "retry"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.12.2",
    "winston-transport": "^4.9.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "winston": "^3.17.0"
  }
}
